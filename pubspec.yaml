name: ddns_updater
description: A sample command-line application.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=2.19.6 <3.0.0"

dependencies:
  http: ^0.13.6

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
